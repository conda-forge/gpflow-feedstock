{% set version = "2.5.1" %}

package:
  name: gpflow
  version: {{ version }}

source:
  fn: GPflow-{{ version }}.tar.gz
  url: https://github.com/GPflow/GPflow/archive/v{{ version }}.tar.gz
  sha256: a97934043d3480a6ee3dbdf0e188f0741ba7d8164d68c9ff8b19c5b6f757a8de

build:
  number: 0
  skip: true  # [win]
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python
    - pip
  run:
    - python
    - numpy
    - scipy
    - tensorflow-base >=2,<3
    - tensorflow-probability >=0.12.0,<0.13.0
    - multipledispatch >=0.6
    - tabulate
    - typing_extensions
    - packaging
    - deprecated
    - lark

test:
  imports:
    - gpflow

about:
  home: https://github.com/GPflow/GPflow/
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: Gaussian processes in TensorFlow

extra:
  recipe-maintainers:
    - cxhernandez

{% set version = "2.4.0" %}

package:
  name: gpflow
  version: {{ version }}

source:
  fn: GPflow-{{ version }}.tar.gz
  url: https://github.com/GPflow/GPflow/archive/v{{ version }}.tar.gz
  sha256: 7ba9f48b446f2010ad82bdae83150895da17f5106c891a49c3905826b5902cf7

build:
  number: 0
  skip: true  # [win]
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python
    - pip
  run:
    - python
    - numpy
    - scipy
    - tensorflow-base >=2,<3
    - tensorflow-probability >=0.12.0,<0.13.0
    - multipledispatch >=0.6
    - tabulate
    - typing_extensions
    - packaging
    - deprecated
    - lark

test:
  imports:
    - gpflow

about:
  home: https://github.com/GPflow/GPflow/
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: Gaussian processes in TensorFlow

extra:
  recipe-maintainers:
    - cxhernandez
    - jschueller

{% set name = "gpflow" %}
{% set version = "2.9.1" %}

package:
  name: gpflow
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/gpflow-{{ version }}.tar.gz
  sha256: bb1f92eac890ba79bfddc94bb4b4e8ee92c636ae07b1a5e0e6dfce92ee6d5634

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - check_shapes>=1.0.0
    - deprecated
    - lark >=1.1.0
    - multipledispatch >=0.6
    - numpy
    - packaging
    - python >=3.7
    - scipy
    - setuptools >=41.0.0
    - tabulate
    - tensorflow >=2.4.0
    - tensorflow-probability >=0.12.0
    - typing-extensions

test:
  imports:
    - gpflow
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/GPflow/GPflow/
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: Gaussian processes in TensorFlow

extra:
  recipe-maintainers:
    - cxhernandez
